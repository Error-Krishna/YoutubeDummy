import { asyncHandler } from "../utils/asyncHandler.js";
import { apiError } from "../utils/apiError.js";
import { uploadOnCloudinary } from "../utils/cloudinary.js";
import { apiResponse } from "../utils/apiResponse.js";
import jwt from "jsonwebtoken";
import mongoose from "mongoose";


const getAllVideoComments = asyncHandler(async (requestAnimationFrame, res) => {

});
const addComments = asyncHandler(async (requestAnimationFrame, res) => {

});
const updateComments = asyncHandler(async (requestAnimationFrame, res) => {

});
const deleteComments = asyncHandler(async (requestAnimationFrame, res) => {

});
export {
    getAllVideoComments,
    addComments,
    updateComments,
    deleteComments,
}
