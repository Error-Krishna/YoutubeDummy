import { asyncHandler } from "../utils/asyncHandler.js";
import { apiError } from "../utils/apiError.js";
import { uploadOnCloudinary } from "../utils/cloudinary.js";
import { apiResponse } from "../utils/apiResponse.js";
import jwt from "jsonwebtoken";
import mongoose from "mongoose";


const createPlaylist = asyncHandler(async (requestAnimationFrame, res) => {

});
const getUserPlaylists = asyncHandler(async (requestAnimationFrame, res) => {

});
const getPlaylistById = asyncHandler(async (requestAnimationFrame, res) => {

});
const addVideoToPlaylist = asyncHandler(async (requestAnimationFrame, res) => {

});
const removeVideoFromPlaylist = asyncHandler(async (requestAnimationFrame, res) => {

});
const deletePlaylist = asyncHandler(async (requestAnimationFrame, res) => {

});
const updatePlaylist = asyncHandler(async (requestAnimationFrame, res) => {

});
export {
    createPlaylist,
    getUserPlaylists,
    getPlaylistById,
    addVideoToPlaylist,
    removeVideoFromPlaylist,
    deletePlaylist,
    updatePlaylist ,
}
